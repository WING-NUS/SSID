<%= form_for(@submission_cluster) do |f| %>
  <% if @submission_cluster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submission_cluster.errors.count, "error") %> prohibited this submission_cluster from being saved:</h2>

      <ul>
      <% @submission_cluster.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
