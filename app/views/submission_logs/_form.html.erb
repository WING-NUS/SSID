<%= form_for(@submission_log) do |f| %>
  <% if @submission_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submission_log.errors.count, "error") %> prohibited this submission_log from being saved:</h2>

      <ul>
      <% @submission_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
