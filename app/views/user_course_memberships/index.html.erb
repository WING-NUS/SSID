<h1>Listing user_course_memberships</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_course_memberships.each do |user_course_membership| %>
  <tr>
    <td><%= link_to 'Show', user_course_membership %></td>
    <td><%= link_to 'Edit', edit_user_course_membership_path(user_course_membership) %></td>
    <td><%= link_to 'Destroy', user_course_membership, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User course membership', new_user_course_membership_path %>
