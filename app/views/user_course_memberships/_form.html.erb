<%= form_for(@user_course_membership) do |f| %>
  <% if @user_course_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_course_membership.errors.count, "error") %> prohibited this user_course_membership from being saved:</h2>

      <ul>
      <% @user_course_membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
